#include<iostream>
#include<algorithm>
using namespace std;

const int MAXN=30;
int a[MAXN][3];
int b[MAXN][2];

int main()
{
	int n;
	cin>>n;
	for(int i=0;i<n;i++)
	{
		for(int j=0;j<1;j++)
		{
		cin>>a[i][j];
		a[i][2]=i;
		}
	}
	for(int i=0;i<n;i++)
	{
	b[i][0]=a[i][0]*a[i][1];
	b[i][1]=a[i][2];
	}
	for(int i=0;i<n;i++)
	if(b[])
	for(int i=0;i<n;i++)
	cout<<
}